#this is for the user input and convert to int
userNumber = int(input("Whats your Number:"))

#lambda multiply function
multiply = lambda a: a * 10

#3will get the user number multiply by lambda function 10
print(multiply(userNumber))
