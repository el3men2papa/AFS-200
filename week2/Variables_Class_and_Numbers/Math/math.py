#this will import all python math
from math import pi

#it will conver the input number for calculation also asking for the number
r = float(input ("Input the radius of the circle : "))

#it will print the number and also will give the calculation of the circle
print ("The area of the circle with radius " + str(r) + " is: " + str(pi * r**2))